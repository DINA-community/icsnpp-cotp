This plugin provides a protocol analyzer for COTP (ISO 8073 / X.224) for use
within Zeek. The analyzer enables Zeek to parse COTP PDUs and trigger further
protocol parsing of SESSION SPDUs, if the SESS analyzer is available.
